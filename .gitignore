node_modules/
dist/
out/
.DS_Store
*.local
coverage/
TODO.md

# Environment files — .env and .env.example are committed (safe defaults only)
# Put real secrets in .env.local — never commit it
.env.local
.env.*.local
.env

private/
# MCP server config — carries auth headers/tokens for github and supabase.
# Local-only; never commit it.
.mcp.json
